<div class="bg-gray-100 p-6 rounded-md shadow-md max-w-md mx-auto">
  <h2 class="text-xl font-bold mb-4">Registrar Área de Intervención</h2>
  <form (ngSubmit)="onSubmit()">
    <div class="mb-4">
      <label for="name" class="block text-gray-700">Nombre del área</label>
      <input type="text" id="name" [(ngModel)]="name" name="name" required class="border border-gray-300 rounded w-full py-2 px-3">
    </div>
    <div class="mb-4">
      <label for="description" class="block text-gray-700">Descripción</label>
      <textarea id="description" [(ngModel)]="description" name="description" required class="border border-gray-300 rounded w-full py-2 px-3"></textarea>
    </div>
    <div *ngIf="errorMessage" class="text-red-500 mb-4">{{ errorMessage }}</div>
    <div class="flex justify-between">
      <button type="submit" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-700">Guardar</button>
      <button type="button" class="bg-gray-300 text-gray-700 px-4 py-2 rounded hover:bg-gray-400">Cancelar</button>
    </div>
  </form>
</div>
